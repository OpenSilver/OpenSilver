<sdk:Page x:Class="TestApplication.Tests.LinQTest"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          mc:Ignorable="d"
          xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk">

    <StackPanel Orientation="Vertical">

        <DockPanel Margin="20">

            <Border Background="#99FFFFFF"
                    BorderBrush="#CCCCFF"
                    BorderThickness="2"
                    Margin="0,0,0,20"
                    Padding="20"
                    DockPanel.Dock="Top">
                <TextBlock Width="750"
                           TextWrapping="Wrap">
        This example animates an object (rectangle) across the screen along a path. To do this, the X and Y property values are animated along a PathGeometry
        using DoubleAnimationUsingPath. The DoubleAnimationUsingPath object is needed because the X and Y properties use double values.
                </TextBlock>
            </Border>


            <StackPanel DockPanel.Dock="Left">

                <!-- Canvas Containing the animated object and the path it follows during animation -->
                <Canvas HorizontalAlignment="Left"
                        Margin="0"
                        Width="340"
                        Height="215">

                    <!-- This Path is only to show the path that the animated object will follow. -->
                    <Path VerticalAlignment="Top"
                          Margin="15,15,15,15"
                          Data="M 10,100 C 35,0 135,0 160,100 180,190 285,200 310,100"
                          Stroke="Black"
                          StrokeThickness="2"
                          Stretch="None" />

                    <!-- This geometry renders the rectangle that is animated across the screen. -->
                    <Path Fill="Blue"
                          Margin="0,0,15,15">
                        <Path.Data>
                            <RectangleGeometry  x:Name="MyAnimatedRectGeometry"
                                                Rect="0,0 30 30" />
                        </Path.Data>
                        <Path.RenderTransform>
                            <TranslateTransform x:Name="MyAnimatedTransform"
                                                X="10"
                                                Y="100" />
                        </Path.RenderTransform>
                    </Path>
                </Canvas>


                <!-- Create button to restart the animations. -->
                <Button Width="80"
                        HorizontalAlignment="Left">Start Animations

                    <!-- Trigger and StoryBoard to initiate the animation when the button is clicked. -->
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.Click">
                            <BeginStoryboard Name="MyBeginStoryboardDouble">
                                <Storyboard>

                                    <!-- Animates the rectangle horizotally along the path. -->
                                    <DoubleAnimationUsingPath Storyboard.TargetName="MyAnimatedTransform"
                                                              Storyboard.TargetProperty="X"
                                                              Source="X"
                                                              Duration="0:0:5"
                                                              RepeatBehavior="Forever"
                                                              AutoReverse="True">
                                        <DoubleAnimationUsingPath.PathGeometry>
                                            <PathGeometry Figures="M 10,100 C 35,0 135,0 160,100 180,190 285,200 310,100" />
                                        </DoubleAnimationUsingPath.PathGeometry>
                                    </DoubleAnimationUsingPath>

                                    <!-- Animates the rectangle vertically along the path. -->
                                    <DoubleAnimationUsingPath Storyboard.TargetName="MyAnimatedTransform"
                                                              Storyboard.TargetProperty="Y"
                                                              Source="Y"
                                                              Duration="0:0:5"
                                                              RepeatBehavior="Forever"
                                                              AutoReverse="True">
                                        <DoubleAnimationUsingPath.PathGeometry>
                                            <PathGeometry Figures="M 10,100 C 35,0 135,0 160,100 180,190 285,200 310,100" />
                                        </DoubleAnimationUsingPath.PathGeometry>
                                    </DoubleAnimationUsingPath>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Button.Triggers>
                </Button>

            </StackPanel>
        </DockPanel>

        <DockPanel Margin="20">
            <Border Background="#99FFFFFF"
                    BorderBrush="#CCCCFF"
                    BorderThickness="2"
                    Margin="0,0,0,20"
                    Padding="20"
                    DockPanel.Dock="Top">
                <TextBlock Width="750">
          This example animates an object (rectangle) across the screen along a path. To do this, the Center property value is animated along a PathGeometry
          using PointAnimationUsingPath. The PointAnimationUsingPath object is needed because the Center property uses a point value.
                </TextBlock>
            </Border>

            <StackPanel DockPanel.Dock="Left">
                <Canvas HorizontalAlignment="Left"
                        Width="340"
                        Height="240">

                    <!-- This Path is only to show the path that the animated object will follow. -->
                    <Path VerticalAlignment="Top"
                          Margin="15,15,15,15"
                          Data="M 10,100 C 35,0 135,0 160,100 180,190 285,200 310,100"
                          Stroke="Black"
                          StrokeThickness="2"
                          Stretch="None" />

                    <Path Fill="Blue"
                          Margin="15,15,15,15">
                        <Path.Data>

                            <!-- Describes an ellipse. -->
                            <EllipseGeometry x:Name="MyAnimatedEllipseGeometry"
                                             Center="10,100"
                                             RadiusX="15"
                                             RadiusY="15" />
                        </Path.Data>
                    </Path>
                </Canvas>


                <!-- Create a button to restart the animation. -->
                <Button Width="80"
                        HorizontalAlignment="Left">Start Animation

                    <!-- Trigger and StoryBoard to initiate the animation when the button is clicked. -->
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.Click">
                            <BeginStoryboard Name="MyBeginStoryboardPoint">
                                <Storyboard>

                                    <!-- Animates the ellipse along the path. -->
                                    <PointAnimationUsingPath Storyboard.TargetName="MyAnimatedEllipseGeometry"
                                                             Storyboard.TargetProperty="Center"
                                                             Duration="0:0:5"
                                                             RepeatBehavior="Forever"
                                                             AutoReverse="True">
                                        <PointAnimationUsingPath.PathGeometry>
                                            <PathGeometry Figures="M 10,100 C 35,0 135,0 160,100 180,190 285,200 310,100" />
                                        </PointAnimationUsingPath.PathGeometry>
                                    </PointAnimationUsingPath>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Button.Triggers>
                </Button>
            </StackPanel>
        </DockPanel>

        <DockPanel Margin="20">
            <Border Background="#99FFFFFF"
                    BorderBrush="#CCCCFF"
                    BorderThickness="2"
                    Margin="0,0,0,20"
                    Padding="20"
                    DockPanel.Dock="Top">
                <TextBlock Width="750">
          This example animates an object (button) across the screen along a path. To do this, the Matrix value of the RenderTransform property is animated 
          along a PathGeometry. MatrixAnimationUsingPath is used to animate the matrix value.
                </TextBlock>
            </Border>

            <StackPanel DockPanel.Dock="Left">
                <Canvas HorizontalAlignment="Left"
                        Width="340"
                        Height="240">

                    <!-- This Path is only to show the path that the animated object will follow. -->
                    <Path VerticalAlignment="Top"
                          Margin="15,15,15,15"
                          Data="M 10,100 C 35,0 135,0 160,100 180,190 285,200 310,100"
                          Stroke="Black"
                          StrokeThickness="2"
                          Stretch="None" />

                    <!-- The Button that is animated across the screen. -->
                    <Path Fill="Blue"
                          Margin="0,0,15,15">
                        <Path.Data>
                            <RectangleGeometry  x:Name="MyAnimatedRectGeometryMatrix"
                                                Rect="0,0 30 30" />
                        </Path.Data>
                        <Path.RenderTransform>
                            <MatrixTransform x:Name="myMatrixTransform">
                                <MatrixTransform.Matrix>
                                    <Matrix OffsetX="10"
                                            OffsetY="100" />
                                </MatrixTransform.Matrix>
                            </MatrixTransform>
                        </Path.RenderTransform>
                    </Path>
                </Canvas>

                <!-- Create a button to restart the animation. -->
                <Button Width="300"
                        HorizontalAlignment="Left">
                    Start Animation

                    <!-- Trigger and StoryBoard to initiate the animation when the button is clicked. -->
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.Click">
                            <BeginStoryboard>
                                <Storyboard>

                                    <!-- Animates the button along the path. -->
                                    <MatrixAnimationUsingPath Storyboard.TargetName="myMatrixTransform"
                                                              Storyboard.TargetProperty="Matrix"
                                                              Duration="0:0:5"
                                                              RepeatBehavior="Forever"
                                                              AutoReverse="True">
                                        <MatrixAnimationUsingPath.PathGeometry>
                                            <PathGeometry Figures="M 10,100 C 35,0 135,0 160,100 180,190 285,200 310,100" />
                                        </MatrixAnimationUsingPath.PathGeometry>
                                    </MatrixAnimationUsingPath>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Button.Triggers>
                </Button>

                <!-- This button starts an animation that follows the tangent
    of the path because DoesRotateWithTangent is set to "True".-->
                <Button Width="300"
                        HorizontalAlignment="Left">
                    Start Animation with DoesRotateWithTangent="True"

                    <!-- Trigger and StoryBoard to initiate the animation when the button is clicked. -->
                    <Button.Triggers>
                        <EventTrigger RoutedEvent="Button.Click">
                            <BeginStoryboard>
                                <Storyboard>

                                    <!-- Animates the button along the path following the tangent of the path. -->
                                    <MatrixAnimationUsingPath Storyboard.TargetName="myMatrixTransform"
                                                              Storyboard.TargetProperty="Matrix"
                                                              DoesRotateWithTangent="True"
                                                              Duration="0:0:5"
                                                              RepeatBehavior="Forever"
                                                              AutoReverse="True">
                                        <MatrixAnimationUsingPath.PathGeometry>
                                            <PathGeometry Figures="M 10,100 C 35,0 135,0 160,100 180,190 285,200 310,100" />
                                        </MatrixAnimationUsingPath.PathGeometry>
                                    </MatrixAnimationUsingPath>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Button.Triggers>
                </Button>

            </StackPanel>
        </DockPanel>

        <Button Background="#DDDDDD" Click="TestButtonLinq_Click" Content="Click to test Linq (look at output pane or console output)" HorizontalAlignment="Left"/>
        <TextBlock Text="Below is an example of GroupBy with a list of elements from 2 to 6 grouped with a test checking whether they are above 3 or not:" Margin="0,10,0,0"/>
        <StackPanel x:Name="GroupByStackPanel" Orientation="Horizontal" Margin="5"/>
    </StackPanel>
</sdk:Page>
