<sdk:Page x:Class="TestApplication.Tests.PlaneProjectionTest" 
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Controls Panel -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="10">
            <TextBlock Text="PlaneProjection Test" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            
            <!-- Rotation X -->
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Rotation X:" Width="100"/>
                <Slider x:Name="RotationXSlider" Width="200" Minimum="-90" Maximum="90" Value="0" 
                        ValueChanged="RotationSlider_ValueChanged"/>
                <TextBlock x:Name="RotationXValue" Text="0" Width="50"/>
            </StackPanel>
            
            <!-- Rotation Y -->
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Rotation Y:" Width="100"/>
                <Slider x:Name="RotationYSlider" Width="200" Minimum="-90" Maximum="90" Value="0" 
                        ValueChanged="RotationSlider_ValueChanged"/>
                <TextBlock x:Name="RotationYValue" Text="0" Width="50"/>
            </StackPanel>
            
            <!-- Rotation Z -->
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Rotation Z:" Width="100"/>
                <Slider x:Name="RotationZSlider" Width="200" Minimum="-180" Maximum="180" Value="0" 
                        ValueChanged="RotationSlider_ValueChanged"/>
                <TextBlock x:Name="RotationZValue" Text="0" Width="50"/>
            </StackPanel>
            
            <!-- Global Offset X -->
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Global Offset X:" Width="100"/>
                <Slider x:Name="GlobalOffsetXSlider" Width="200" Minimum="-100" Maximum="100" Value="0" 
                        ValueChanged="RotationSlider_ValueChanged"/>
                <TextBlock x:Name="GlobalOffsetXValue" Text="0" Width="50"/>
            </StackPanel>
            
            <!-- Global Offset Y -->
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Global Offset Y:" Width="100"/>
                <Slider x:Name="GlobalOffsetYSlider" Width="200" Minimum="-100" Maximum="100" Value="0" 
                        ValueChanged="RotationSlider_ValueChanged"/>
                <TextBlock x:Name="GlobalOffsetYValue" Text="0" Width="50"/>
            </StackPanel>
            
            <!-- Reset Button -->
            <Button Content="Reset" Width="100" Margin="0,10,0,0" Click="ResetButton_Click"/>
        </StackPanel>
        
        <!-- Test Elements -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Test Rectangle with PlaneProjection -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Padding="20" Background="#F5F5F5">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Rectangle with PlaneProjection" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <Rectangle x:Name="TestRectangle" 
                               Width="150" Height="100" 
                               Fill="Blue" 
                               HorizontalAlignment="Center">
                        <Rectangle.Projection>
                            <PlaneProjection x:Name="RectangleProjection" RotationY="0"/>
                        </Rectangle.Projection>
                    </Rectangle>
                </StackPanel>
            </Border>
            
            <!-- Test Button with PlaneProjection -->
            <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="20" Background="#F5F5F5">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Button with PlaneProjection" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <Button x:Name="TestButton" 
                            Content="Click Me!" 
                            Width="150" Height="50" 
                            Click="TestButton_Click">
                        <Button.Projection>
                            <PlaneProjection x:Name="ButtonProjection" RotationX="0"/>
                        </Button.Projection>
                    </Button>
                    <TextBlock x:Name="ClickCount" Text="Clicks: 0" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</sdk:Page>

