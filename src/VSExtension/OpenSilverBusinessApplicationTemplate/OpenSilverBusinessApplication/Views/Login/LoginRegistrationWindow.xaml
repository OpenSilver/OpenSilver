<controls:ChildWindow x:Class="$ext_safeprojectname$.LoginUI.LoginRegistrationWindow"
                      x:Name="childWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:local="clr-namespace:$ext_safeprojectname$.LoginUI"
                      xmlns:controls="clr-namespace:System.Windows.Controls;assembly=OpenSilver"
                      MinWidth="400"
                      Style="{StaticResource LogRegWindowStyle}"
                      Closing="LoginWindow_Closing">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <local:LoginForm x:Name="loginForm" Grid.Row="0" />
        <local:RegistrationForm x:Name="registrationForm" Grid.Row="0" />
    </Grid>
</controls:ChildWindow>